<%= render 'shared/banner' %> <br>
<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list), class: "text-color-blue text-decoration-none" %>
    </li>
  <% end %>
</ul>

<%= link_to "Add a new list", new_list_path, class: "btn btn-secondary m-3" %>
